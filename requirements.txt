# Scoring scripts. transformers/torch are only needed for the two model-based
# stages; evaluate.py and normalize_genes.py run with numpy alone.
numpy>=1.24
torch>=2.1
transformers>=4.44

# Notebook (optional): notebooks/gene_disease_ranking.ipynb
# jupyterlab
# pandas       # あれば結果を表で表示、無ければテキストで代替
